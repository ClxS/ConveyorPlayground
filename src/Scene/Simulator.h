#pragma once

#include <vector>
#include "Grid.h"
#include "Sequence.h"

namespace cpp_conv
{
    struct SceneContext;

    namespace simulation
    {
        void simulate(SceneContext& kContext);
    }
}
