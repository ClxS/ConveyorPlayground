#pragma once

#include <vector>
#include "EntityGrid.h"
#include "Sequence.h"

namespace cpp_conv
{
    struct SceneContext;

    namespace simulation
    {
        void simulate(SceneContext& kContext);
    }
}
