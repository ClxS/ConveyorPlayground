#pragma once

enum class Direction
{
    Left,
    Up,
    Right,
    Down,
};

enum class RelativeDirection
{
    Forward,
    Backwards,
    Right,
    Left
};

enum class EntityKind
{
    Conveyor
};